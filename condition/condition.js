let a =1;
//  1.declare: let a;
//  2.binding: a = 1;
let b= 2;
if(a> b){
    console.log("hello a");
} else if (b === a){
    console.log("hello a and b");
} else{
    console.log("hello b");
}

// = : bind type of variable
// == : equality => transfer  both to same type and compare
// === :strict equality => not transfer and conpare type of them